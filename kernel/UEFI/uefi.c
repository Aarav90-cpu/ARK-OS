#include "../SHARED/klog.h"
#include "../SHARED/subsystems.h"

void UEFI_Init(BootInfo *binfo) {
    (void)binfo;
    SystemLog("IN PROGRESS", "Parsing UEFI BootInfo & Memory Map...");
    SystemLog("PASS", "Parsing UEFI BootInfo & Memory Map...");
}

